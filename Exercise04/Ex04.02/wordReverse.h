
/**
* @brief prints the words of a string in reversed order
* @param str string to be word reversed
*/
void wordReverse(char *str);

/**
* @brief prints a string 
* @param str string to be printed
*/
void printStr(char *str);

/**
* @brief counts the characters of the first word in a string
* @param str string to be counted
* @return the amount of characters in the first word in the string
*/
int wordSize(char *str);