
/**
* @brief swaps to integer values
* @param num1 pointer to the first number
* @param num2 pointer to the second number
*/
void swap(int *num1, int *num2);

/**
* @brief transposes the matrix in place
* @param matrix 2d array containing the matrix
* @param dim the dimension of the matrix
*/
void transposeMatrix(int **matrix, int dim);
