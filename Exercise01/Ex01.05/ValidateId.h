#include<stdio.h>

/**
* Function that checks whether an ID is a valid ID based on the check bit
* @param an ID number
* @return True if the ID is valid, False if the ID is not a valid ID
*/
int checkIDValidity(int id);

/**
* Calculates the amount of digits that are in the integer number
* @param an integer number
* @return amount of digits in the number
*/
int numLength(int num);