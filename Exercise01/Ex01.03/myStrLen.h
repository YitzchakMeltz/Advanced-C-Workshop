
/**
* Function return the length of the given string
* 
* Iterates through the string with a counter until it gets to a NULL character
* 
* @param C style string
* 
* @return length of the string
*/
int myStrLen(const char*);